---
- name: Provisionamento de uma workstation
  hosts: localhost
  connection: local

  roles:
    - role: workstation
      tags: workstation
    - role: chrome
      tags: chrome
    - role: virtualbox
      tags: vbox
    - role: vagrant
      tags: vagrant
    - role: spotify
      tags: spotify
    - role: postgres
      tags: postgres
    - role: docker
      tags: docker
      docker_install_compose: true
      docker_compose_version: "1.14.0"
